language: php

dist: precise
sudo: false

matrix:
  include:
    - php: 5.6
    - php: 7.0
    - php: 7.1
  fast_finish: true

before_script:
  - composer update

script:
  - vendor/bin/phpunit

after_success:
  - travis_retyr php vendor/bin/coveralls
